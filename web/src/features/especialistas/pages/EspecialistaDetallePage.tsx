import { PageShell } from "../../../shared";

const EspecialistaDetallePage = () => {
	return (
		<PageShell
			title="Perfil del especialista"
			description="Vista de detalles y calendario de disponibilidad."
		/>
	);
};

export default EspecialistaDetallePage;
