import { PageShell } from "../../../shared";

const DisponibilidadPublicaPage = () => {
	return (
		<PageShell
			title="Disponibilidad"
			description="Calendario publico de bloques disponibles por especialista."
		/>
	);
};

export default DisponibilidadPublicaPage;
