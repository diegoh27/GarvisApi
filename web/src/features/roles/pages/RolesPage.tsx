import { PageShell } from "../../../shared";

const RolesPage = () => {
	return (
		<PageShell
			title="Roles"
			description="Listado y configuracion de roles."
		/>
	);
};

export default RolesPage;
