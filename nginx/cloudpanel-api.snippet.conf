# CloudPanel — sitio api.garbis.online
# En el panel: App Port = 3001 (debe coincidir con PORT en api/.env)
# Si el preflight devuelve 502, la API no está escuchando en ese puerto: pm2 logs garvis-api

# Opcional: cabeceras CORS en nginx (respaldo si Node no responde OPTIONS)
# location / {
#   if ($request_method = OPTIONS) {
#     add_header Access-Control-Allow-Origin "https://garbis.online";
#     add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, PATCH, OPTIONS";
#     add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With, Accept";
#     add_header Access-Control-Allow-Credentials "true";
#     add_header Content-Length 0;
#     return 204;
#   }
#   proxy_pass http://127.0.0.1:3001/;
#   ...
# }
